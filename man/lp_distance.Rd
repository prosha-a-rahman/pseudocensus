% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lp_distance.R
\name{lp_distance}
\alias{lp_distance}
\title{Compute distances between points using p-norm metrics}
\usage{
lp_distance(reference_point, point_matrix, p = 2)
}
\arguments{
\item{reference_point}{A numeric vector representing the reference point from
which distances are measures}

\item{point_matrix}{A numeric matrix with \code{length(reference_point)} rows,
where each column represents a point in the same vector space as
\code{reference_point}.}

\item{p}{A double >= 1 (or \code{Inf}) specifying which \code{p}-norm to use for
distance calculations. The deafult is \code{p = 2} (Euclidean distances).}
}
\value{
A length \code{ncol(point_matrix)} numeric vector of non-negative distances.
}
\description{
Calculates the distance of a set of points to a reference point with respect
to the vector \code{p}-norm.
}
\details{
\code{lp_distance()} computes the distance between the specified \code{reference_point}
and each point represented by the columns of \code{point_matrix} using the vector
\code{p}-norm.

For finite \code{p}, the \code{p}-norm distance between vectors \eqn{x} and \eqn{y} is
\deqn{||x - y||_p = \left(\sum_{i=1}^{n} |x_i - y_i|^p\right)^{1/p}.}

For \code{p = Inf}, the distance is the maximum of the absolute differences:
\deqn{||x - y||_{\infty} = \max_{i} |x_i - y_i|}

Common p-norm metrics:
\itemize{
\item \code{p = 1}: Manhattan/Taxicab distance (sum of absolute differences)
\item \code{p = 2}: Euclidean distance (straight-line distance)
\item \code{p = Inf}: Chebyshev distance (maximum absolute difference)
}

When \code{reference_point} is the zero vector, \code{lp_distance()} calculates the
\code{p}-norm of each point in the \code{point_matrix}.
}
