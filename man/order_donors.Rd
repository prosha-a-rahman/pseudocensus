% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/order_donors.R
\name{order_donors}
\alias{order_donors}
\title{Rank donors by distance}
\usage{
order_donors(recipient, donors, locations, p = 2, k = NULL)
}
\arguments{
\item{recipient}{Index of the recipient.}

\item{donors}{Index vector of donors.}

\item{locations}{Numeric matrix identifying rowwise location of indexes.}

\item{p}{Number >= 1 (or \code{Inf}) specifying which \code{p}-norm to use for
distance calculations. The default is \code{p = 2} (Euclidean distances).}

\item{k}{Positive integer indicating if only the \code{k} nearest donors should
be returned. If \code{k = NULL} (default), then all \code{donors} are ranked.}
}
\value{
An integer vector containing a permuted subset of \code{donors}, ordered
according to their ascendingly ranked distance from the \code{recipient}. If \code{k}
is specified, only the \code{k} closest donors are returned, otherwise
\code{order_donors()} returns a permutation of \code{donors}.
}
\description{
Order donors according to their proximity to a recipient.
}
\details{
\code{order_donors()} ascendingly ranks the set of \code{donors} by their proximity to
the \code{recipient}. Distances are calculated with respect to the rowwise values
in \code{locations}. If \code{k} is specified, then only the \code{k} nearest donors to the
\code{recipient} are returned, otherwise the entire set of donors are ranked.

For finite \code{p}, the \code{p}-norm distance between vectors \eqn{x} and \eqn{y} is
\deqn{||x - y||_p = \left(\sum_{i=1}^{n} |x_i - y_i|^p\right)^{1/p}.}

For \code{p = Inf}, the distance is the maximum of the absolute differences:
\deqn{||x - y||_{\infty} = \max_{i} |x_i - y_i|.}

Common \code{p}-norm metrics:
\itemize{
\item \code{p = 1}: Manhattan/taxicab distance (sum of absolute differences)
\item \code{p = 2}: Euclidean distance (straight-line distance)
\item \code{p = Inf}: Chebyshev distance (maximum absolute difference)
}

\code{order_donors()} uses \code{Rfast::dista()} implementation for fast distance
calculations.
}
\seealso{
\code{\link[Rfast:dista]{Rfast::dista()}} for the underlying distance calculation.
}
